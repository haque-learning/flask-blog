[phases.setup]
nixPkgs = ["postgresql", "python3", "gcc"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install --no-cache-dir -r requirements.txt"
]

[phases.start]
cmd = "gunicorn app:app"